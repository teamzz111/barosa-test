import {SafeAreaView, StatusBar, Text} from 'react-native';

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <Text>Home Screen</Text>
      <StatusBar />
    </SafeAreaView>
  );
};

export default HomeScreen;
